import { GameObject } from 'game-worldmap-generator';

export class GameObjectPropExtractor {
    private gameObject: GameObject;

    constructor(gameObject: GameObject) {
        this.gameObject = gameObject
    }
}